---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<p class={`eyebrow text-micro ${className}`}>
  <slot />
</p>

<style>
  .eyebrow {
    color: var(--color-gold);
    margin-bottom: var(--sp-2);
  }
</style>
